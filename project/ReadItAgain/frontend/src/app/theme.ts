"use client";
import { createTheme } from '@mui/material/styles';

const riatheme =  createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#c8ab83',
    },
    secondary: {
      main: '#813405',
    },
  },
});

export default riatheme;